import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    padding: 5,
  },

  title: {
    fontSize: 22,
    fontWeight: 'bold',
  },
});
